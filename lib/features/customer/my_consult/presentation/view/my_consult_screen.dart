import 'package:flutter/material.dart';

class MyConsultScreen extends StatelessWidget {
  const MyConsultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('My Consult'));
  }
}
